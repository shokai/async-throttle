machine:
  node:
    version: 8.9.3
  post:
    - node -v
    - npm -v

dependencies:
  override:
    - npm install

test:
  override:
    - npm run build
    - npm run test:mocha
    - npm run test:standard
